void ReadFiles(boost::filesystem::path path) {
	//boost::filesystem::recursive_directory_iterator it = createRIterator(path);
    boost::filesystem::recursive_directory_iterator end;
};*/

/*bool find_file (const path & dirPath,         // in this directory,
				const std::string & fileName, // search for this name,
				path & pathFound )  {          // placing path here if found
		if (!exists(dirPath)) return false;
		directory_iterator endItr; // default construction yields past-the-end
		for ( directory_iterator itr(dirPath); itr != endItr; ++itr ) {
				if (is_directory(itr->status())) {
					if ( find_file( itr->path(), fileName, pathFound ) ) return true;
				}
				else if ( (itr->leaf) == fileName )  {
					pathFound = itr->path();
					return true;
				}
		}
		return false;
}*/
